Use the run configs in the .run folder with intellij

# How to package into war
- Use .run/MvnPackage in intellij

# Linux setup - Prerequisits
- sudo apt-get update
- sudo apt-get install tesseract-ocr
- sudo apt-get install libtesseract-dev